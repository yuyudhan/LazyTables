
====================****====================
DONE:
What is the purpose of the normal mode and query mode? Is it something that we are doing right? Or is there a better way? Because what I feel is, given that for addition of connections, addition of tables and all, we are going to have a different view. Is this the right structure that we are even doing or we can do better? Because I think it is complicating stuff. Can you just validate it for me?

====================****====================
DONE:
I think we have over complicated the management of the state. So all we need is a six pin layout, uh, with tab numbers and pressing that number should take us to that tab. Uh, we, uh, try to implement an insert mode. So this is useful only for the SQL query editor tab. Others are like plain tabs, which should just have key bindings. Let's say I'm in the corrections plane. uh, in that connection, Spain, if I press a, I should be able to add a connection. It should go into a connection edition mode. Uh, when I press E while on a connection, it should go into an edit connection mode. Uh, pressing escape in those modes should take us back to the main mode. Uh, so this is how we want to simplify our overall architecture and how the lazy tables terminal UI application behaves. Uh, so how can we achieve that? What are the areas where we have to work on and what is the plan to simplify the overall architecture as well as how we are managing the, uh, systems, uh, key bindings and all that stuff. Uh, we need normal edit mode only in the SQL editor and nothing else. Rest can be plain key bindings.

====================****====================
DONE:
I want you to go through the entire implementation of all the 6 panes and all the modes and models. Can you identify if there is anything which is essentially blocking in nature and which can freeze the UI? Do it properly. I want you to go through everything and find out everything that might block the UI in any time. Can you have anything blocking and everything should show some kind of loading or responsiveness in the UI whenever it is there? So, can you find such scenarios? Go through everything and then devise a plan how we can fix it. Which elements are there first of all? Let's find out.

====================****====================
Can you check throughout the application if we have any code which is not used or any of the code which is just stubbing or any code which is not done properly or there are functions which have been written but not being called properly and not binded properly or there are any feature which are residual remaining somewhere or there are missing features which are supposed to be implemented but have been done only in a stubbed manner.

====================****====================
the readme.md seems to be ancient with everything that is old see first of all we have the release as v0.2.1 the version only is wrong all the help that is mentioned is wrong.

can you create the readme file in a manner that is more user friendly so initial part should be on the installations and then go into other things simple installation thing that cargo install the readme.md is kind of the entry point for what the user will see it should be short catchy clear and unnecessary thing which let's say the user does not want to see right then and there should be moved to docs/user/ directory. This docs/user directory can have more files which can just be linked in the readme.md so think properly keep the readme.md updated as per what we have right now and the version that is there so also do it on the release when we do the new release it should get updated so that it reflects the current reality.

====================****====================
The version number to be publioshes is <>.
I want you to create a build for release and publish to crates.io. I have already done the crates log through the terminal command creates login. Update the documentation as need be and add the release notes /change logs / other documentation details which might be needed. Keep things up to date. Keep the markdown content crispy and cleaner. Don't just bloat it with random barbiage. Ensure clarity and focus on the user.

====================****====================

====================****====================

====================****====================

====================****====================

====================****====================

====================****====================

====================****====================

